# S0 — Engine Conformance Probe Results

**Engine**: `claude` 2.1.220 (Claude Code)
**Run**: 2026-07-30T21:02:17.356Z · live calls ENABLED · probe model `haiku`
**Totals**: 28 pass · 0 fail · 2 partial · 0 inconclusive · 0 error · 0 skipped

> Generated by `tools/probe/run.mjs`. Do not hand-edit — re-run the probe instead.

## Findings requiring a decision

### PARTIAL — --agents defines a dispatchable agent inline `p-agents-inline`

- **Claim under test**: Role agents can be injected per session as JSON without shipping files. NOTE: not load-bearing — Guidelane runs each review lens as its own top-level session (ADR-002), so subagent dispatch is a convenience, not a dependency.
- **Load-bearing**: low
- **If false**: None structural. Lenses ship as plugin agents (proven by p-plugin-skill-headless) or as separate sessions, which is the actual design.
- **Observed**: Inline agent marker not observed; the flag may be accepted without the agent being invoked. Output: ## Workspace Findings

**Directory Location:** `<TMP>/guidelane-probe-fxhiet/p-agents-inline`

**Status:** The workspace directory is **empty**

### Key Observations:
- **Directory Size:** 0 bytes (no files)
- **Directory Permissions:** `drwxr-xr-x`…[825 chars]
- **Plan refs**: RESEARCH-01 §4.1, RESEARCH-02 §4 G5

### PARTIAL — The spawned child's auto-updater can be governed `p-autoupdate-governable`

- **Claim under test**: An environment variable or setting disables the auto-updater in child sessions, so a mid-run update cannot change the engine under a running pipeline.
- **Load-bearing**: high
- **If false**: REVIEW-01 #5 has no remedy: every user's engine can change simultaneously, mid-project.
- **Observed**: No auto-update control found in help text. Harness sets DISABLE_AUTOUPDATER=1 defensively; treat governance as UNPROVEN until observed across a release.
- **Plan refs**: REVIEW-01 #5, ADR-001

## All probes

| Probe | Result | Load-bearing | Observed |
|---|---|---|---|
| `p-flag-surface` Every depended-on CLI flag exists | PASS | critical | All 33 required flags present. |
| `p-bare-forbidden` --bare would break subscription auth (so it must never be used) | PASS | critical | Help text confirms API-key-only auth under --bare, verbatim as the plan quotes it. |
| `p-safe-mode-forbidden` --safe-mode disables the customizations Guidelane depends on | PASS | high | --safe-mode help mentions disabling: skills, plugins, hooks, MCP servers. |
| `p-doctor-exists` `claude doctor` exists and can back G0 | PASS | medium | Subcommand present; G0 should wrap it rather than duplicate environment checks. |
| `p-plugin-eval-exists` `claude plugin validate --strict` and `plugin eval` exist | PASS | medium | plugin subcommands — validate: true, eval: true. |
| `p-plugin-validate-fixture` The fixture plugin passes `plugin validate --strict` | PASS | high | Fixture plugin validates strictly (manifest + skills + agents + hooks + bundled MCP). |
| `p-kill-reaps-process-group` A timed-out child takes its whole process tree with it | PASS | critical | Timeout killed the whole group: the direct child AND its grandchild were both gone afterwards. The process-group kill added at S0 close does what it claims. |
| `p-watchdog-settles-on-escaped-child` A grandchild that escapes the process group cannot hang the suite | PASS | critical | Escaped grandchild held the pipe open and 'close' never came, yet the call settled in 8022ms via the watchdog. A stuck session degrades into evidence instead of a silent hang. As designed, the escapee |
| `p-version-in-tested-range` The exercised CLI version is inside the tested range | PASS | high | Engine 2.1.220 is inside the tested range 2.1.220–2.1.999. |
| `p-init-receipt` system/init is a machine-checkable receipt of the whole context package | PASS | critical | init carries every field the orchestrator needs to assert on. apiKeySource=none (the auth-mode discriminator), version=2.1.220. |
| `p-ambient-isolation` A spawned session inherits the operator's whole configuration unless isolated | PASS | critical | --strict-mcp-config alone LEAKS operator config (plugins 4, skills 24, agents 10, permissionMode bypassPermissions); adding --setting-sources "" reduces it to exactly the CLI's built-in floor (16 skil |
| `p-messagedisplay-rewrite` A MessageDisplay hook rewrite reaches stream-json, not just the terminal | PASS | critical | Rewrite propagated into assistant text AND the terminal result — mechanism 4 is real on the product surface. Note: hook failure is fail-open (the engine emits the original text), so the cockpit render |
| `p-stream-json-roundtrip` Bidirectional stream-json round-trip with partial messages and replay | PASS | critical | Full round-trip. Event types: system, rate_limit_event, user, stream_event, assistant, result. |
| `p-usage-accounting` The result event carries usage and cost data | PASS | high | Result keys: is_error, duration_api_ms, num_turns, stop_reason, session_id, total_cost_usd, usage, modelUsage, permission_denials, terminal_reason, fast_mode_state, fast_mode_disabled_reason, subtype, |
| `p-json-schema` --json-schema forces schema-valid structured output | PASS | critical | Structured output matched the schema, including the nested acceptance-criteria array. |
| `p-append-system-prompt` --append-system-prompt reaches the model, and --tools "" removes the toolset | PASS | critical | Appended system prompt changed behaviour in a headless session. |
| `p-agents-inline` --agents defines a dispatchable agent inline | PARTIAL | low | Inline agent marker not observed; the flag may be accepted without the agent being invoked. Output: ## Workspace Findings **Directory Location:** `<TMP> |
| `p-max-budget-subscription` --max-budget-usd behaviour under the operator's current auth | PASS | critical | --max-budget-usd 0.0000001 → enforced (call refused or halted by the budget ceiling). Exit 1. |
| `p-effort-model-fallback` Effort, model, and fallback-model compose in one invocation | PASS | high | Accepted together; model reported as not surfaced in result. |
| `p-permission-allowlist` Headless permissions are fail-closed: auto denies, auto + allow-list permits | PASS | critical | auto alone: DENIED the write (fail-closed, exit 0). auto + --allowedTools Write: wrote the file. |
| `p-session-identity` Session id, resume, and fork behave as the artifact store assumes | PASS | high | Session id honoured, resume kept context, fork produced a new id. |
| `p-mcp-strict-load` Atlas-shaped MCP server loads via --mcp-config with --strict-mcp-config isolation | PASS | critical | MCP server loaded from the command line and its tool was callable under a fail-closed allow-list (mcp__<server>__<tool>). |
| `p-plugin-skill-headless` A session-only plugin loads and its skill is invocable in -p mode | PASS | critical | Session-only plugin loaded and its skill resolved from a headless prompt. |
| `p-hook-events-headless` Which hook lifecycle events actually fire in a headless session | PASS | critical | Fired: SessionStart, UserPromptSubmit, PreToolUse, PostToolUse, MessageDisplay, Stop, SessionEnd \| Assistant marker survived the MessageDisplay hook (empty stdout = no suppression). \| Hook events in |
| `p-plugin-bundled-mcp` A plugin-bundled MCP server loads, and its tools carry the plugin-scoped name | PASS | high | Bundled server loads and is named `mcp__plugin_guidelane-probe_echo__guidelane_probe_echo` — BUT --strict-mcp-config also excludes plugin-bundled servers. Isolation and plugin-bundled delivery are mut |
| `p-version-readable` Engine version is machine-readable for the tested-range check | PASS | high | Parsed version 2.1.220 from `claude --version`. |
| `p-auth-mode-visibility` Auth mode is inspectable without exposing a credential | PASS | high | auth status --json exposes loggedIn, authMethod, subscriptionType — mode is readable without touching a credential. 4 personal field(s) in the same payload must be projected away (ADR-008). |
| `p-automode-classifier-pinned` What `--permission-mode auto` means is the same on every machine | PASS | high | Effective classifier is byte-identical to the shipped defaults (allow:17, soft_deny:65, hard_deny:1, environment:20). |
| `p-autoupdate-governable` The spawned child's auto-updater can be governed | PARTIAL | high | No auto-update control found in help text. Harness sets DISABLE_AUTOUPDATER=1 defensively; treat governance as UNPROVEN until observed across a release. |
| `p-rate-limit-signal` The stream carries a structured rate-limit event with a reset timestamp | PASS | critical | rate_limit_event present — status=allowed, window=five_hour, resetsAt=1785451200 (epoch seconds — supervisor can sleep to the boundary). |

Env deny-list (`CLAUDECODE`, `CLAUDE_CODE_ENTRYPOINT`, `CLAUDE_CODE_SSE_PORT`, `CLAUDE_CODE_SIMPLE`, `CLAUDE_CODE_SAFE_MODE`) applied to all 33 child process(es); actually present and removed: `CLAUDECODE`, `CLAUDE_CODE_ENTRYPOINT`, `CLAUDE_CODE_SSE_PORT`. `DISABLE_AUTOUPDATER=1` and a UTF-8 locale set on every child. Sessions spawned without the ADR-008 isolation pair (deliberately, to measure ambient inheritance): 4.
