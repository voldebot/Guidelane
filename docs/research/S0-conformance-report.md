# S0 — Engine Conformance Probe Results

**Engine**: `claude` 2.1.220 (Claude Code)
**Run**: 2026-07-31T17:56:47.295Z · live calls ENABLED · probe model `haiku`
**Totals**: 33 pass · 0 fail · 0 partial · 0 inconclusive · 0 error · 0 skipped

> Generated by `tools/probe/run.mjs`. Do not hand-edit — re-run the probe instead.

## All probes

| Probe | Result | Load-bearing | Observed |
|---|---|---|---|
| `p-flag-surface` Every depended-on CLI flag exists | PASS | critical | All 33 required flags present AND declared in the option column (69 flags parsed). |
| `p-bare-forbidden` --bare would break subscription auth (so it must never be used) | PASS | critical | Help text confirms API-key-only auth under --bare, verbatim as the plan quotes it. |
| `p-safe-mode-forbidden` --safe-mode disables the customizations Guidelane depends on | PASS | high | --safe-mode help mentions disabling: skills, plugins, hooks, MCP servers. |
| `p-doctor-exists` `claude doctor` exists and can back G0 | PASS | medium | Subcommand present; G0 should wrap it rather than duplicate environment checks. |
| `p-plugin-eval-exists` `claude plugin validate --strict` and `plugin eval` exist | PASS | medium | plugin subcommands — validate: true, eval: true. |
| `p-plugin-validate-fixture` The fixture plugin passes `plugin validate --strict` | PASS | high | Fixture plugin validates strictly (manifest + skills + agents + hooks + bundled MCP). |
| `p-kill-reaps-process-group` A timed-out child takes its whole process tree with it | PASS | critical | Timeout killed the whole group: the direct child AND its grandchild were both gone afterwards. The process-group kill added at S0 close does what it claims. |
| `p-watchdog-settles-on-escaped-child` A grandchild that escapes the process group cannot hang the suite | PASS | critical | Escaped grandchild held the pipe open and 'close' never came, yet the call settled in 8003ms via the watchdog. A stuck session degrades into evidence instead of a silent hang. As designed, the escapee |
| `p-version-in-tested-range` The exercised CLI version is inside the tested range | PASS | high | Engine 2.1.220 is inside the tested range 2.1.220–2.1.999. |
| `p-init-receipt` system/init is a machine-checkable receipt of the whole context package | PASS | critical | init carries every field the orchestrator needs to assert on, and apiKeySource is ASSERTED (=none, the auth-mode discriminator) rather than merely recorded. version=2.1.220. |
| `p-ambient-isolation` A spawned session inherits the operator's whole configuration unless isolated | PASS | critical | --strict-mcp-config alone LEAKS operator config (plugins 4, skills 24, agents 10, permissionMode bypassPermissions); adding --setting-sources "" reduces it to exactly the CLI's built-in floor (16 skil |
| `p-messagedisplay-rewrite` A MessageDisplay hook rewrite reaches stream-json, not just the terminal | PASS | critical | Rewrite propagated into assistant text AND the terminal result — mechanism 4 is real on the product surface. Note: hook failure is fail-open (the engine emits the original text), so the cockpit render |
| `p-stream-json-roundtrip` Bidirectional stream-json round-trip with partial messages and replay | PASS | critical | Full round-trip. Event types: system, user, stream_event, rate_limit_event, assistant, result. |
| `p-usage-accounting` The result event carries usage and cost data | PASS | high | Result keys: is_error, duration_api_ms, num_turns, stop_reason, session_id, total_cost_usd, usage, modelUsage, permission_denials, terminal_reason, fast_mode_state, fast_mode_disabled_reason, subtype, |
| `p-stream-surface-union` Every stream type/subtype pair a session emits is classified in the committed surface artifact | PASS | critical | All 10 pair(s) and 11 stream_event inner type(s) observed in this session are classified, every pinned required pair arrived, and every value-conditional rule this run exercised resolved to a pinned v |
| `p-json-schema` --json-schema forces schema-valid structured output | PASS | critical | Structured output matched the schema, including the nested acceptance-criteria array. |
| `p-append-system-prompt` --append-system-prompt reaches the model, and --tools "" removes the toolset | PASS | critical | Appended system prompt changed behaviour in a headless session. |
| `p-agents-inline` --agents defines a dispatchable agent inline | PASS | low | `--agents` registers inline: the init receipt lists probe_lens beside the 5 built-ins. Dispatch observed: no (not asserted — whether the model elects to call Task is model behaviour, not engine capabi |
| `p-max-budget-subscription` --max-budget-usd behaviour under the operator's current auth | PASS | critical | --max-budget-usd 0.0000001 → enforced (call refused or halted by the budget ceiling). Exit 1. |
| `p-effort-model-fallback` Effort, model, and fallback-model compose in one invocation | PASS | high | Accepted together; model reported as not surfaced in result. |
| `p-permission-allowlist` Headless permissions are fail-closed: auto denies, auto + allow-list permits | PASS | critical | auto alone: DENIED the write (fail-closed, exit 0). Structural denial channels observed: tool_result.is_error. The model still claimed success — a live example of the state-hallucination failure the p |
| `p-session-identity` Session id, resume, and fork behave as the artifact store assumes | PASS | high | Session id honoured, resume kept context, fork produced a new id. |
| `p-mcp-strict-load` Atlas-shaped MCP server loads via --mcp-config with --strict-mcp-config isolation | PASS | critical | MCP server loaded from the command line and its tool was callable under a fail-closed allow-list (mcp__<server>__<tool>). |
| `p-plugin-skill-headless` A session-only plugin loads and its skill is invocable in -p mode | PASS | critical | Session-only plugin loaded and its skill resolved from a headless prompt. |
| `p-hook-events-headless` Which hook lifecycle events actually fire in a headless session | PASS | critical | Fired: SessionStart, UserPromptSubmit, PreToolUse, PostToolUse, MessageDisplay, Stop, SessionEnd \| Assistant marker survived the MessageDisplay hook (empty stdout = no suppression). \| Hook events in |
| `p-plugin-bundled-mcp` A plugin-bundled MCP server loads, and its tools carry the plugin-scoped name | PASS | high | Bundled server registers and its tool round-trips as `mcp__plugin_guidelane-probe_echo__guidelane_probe_echo`; status at init was `connected` (races the handshake — do not gate on it). --strict-mcp-co |
| `p-hook-failure-detectable` A failing hook is distinguishable from a healthy one — and one failure mode is not | PASS | critical | Hook failure is detectable in 2 of 3 modes and INVISIBLE in the third. exit-9 -> exit_code=9/outcome=error (loud); timeout -> exit_code=1/outcome=cancelled (loud, and distinct from error, so a timeout |
| `p-stream-surface-artifact` The committed stream-surface artifact is a usable expectation — checked for free, on every push | PASS | critical | Artifact valid at schemaVersion 2: 11 pair(s), 11 inner type(s), 2 value-conditional rule(s), every one failing closed on an unmodelled value, and defaultForUnknown=escalate. This checks the artifact' |
| `p-version-readable` Engine version is machine-readable for the tested-range check | PASS | high | Parsed version 2.1.220 from `claude --version`. |
| `p-auth-mode-visibility` Auth mode is inspectable without exposing a credential | PASS | high | auth status --json exposes loggedIn, authMethod, subscriptionType — mode is readable without touching a credential. 4 personal field(s) in the same payload must be projected away (ADR-008). |
| `p-automode-classifier-pinned` What `--permission-mode auto` means is the same on every machine | PASS | high | Effective classifier is byte-identical to the shipped defaults (allow:17, soft_deny:65, hard_deny:1, environment:20). Measures THIS machine only — the same assertion on a friend's laptop is a differen |
| `p-autoupdate-governable` The spawned child's auto-updater can be governed | PASS | high | DISABLE_AUTOUPDATER=1 is honoured and observable: "enabled" -> "disabled (set by env: DISABLE_AUTOUPDATER)" (the engine names the env var as the source). The harness sets it on every child, so a mid-r |
| `p-rate-limit-signal` The stream carries a structured rate-limit event with a reset timestamp | PASS | critical | rate_limit_event present — status=allowed, window=five_hour, resetsAt=1785529200 (epoch seconds — supervisor can sleep to the boundary). |

Env deny-list (`CLAUDECODE`, `CLAUDE_CODE_ENTRYPOINT`, `CLAUDE_CODE_SSE_PORT`, `CLAUDE_CODE_SIMPLE`, `CLAUDE_CODE_SAFE_MODE`) applied to all 40 child process(es); actually present and removed: `CLAUDECODE`, `CLAUDE_CODE_ENTRYPOINT`, `CLAUDE_CODE_SSE_PORT`. A UTF-8 locale set on every child, and `DISABLE_AUTOUPDATER=1` on every child except 1 deliberate control-arm spawn(s) (p-autoupdate-governable needs one child without the guard to prove the guard does anything; `claude doctor` installs nothing). Sessions spawned without the ADR-008 isolation pair (deliberately, to measure ambient inheritance): 4. Plus one pre-flight `claude --version` spawned before the audit exists — scrubbed, but not in the count above.

Process hygiene: 0 confirmed live process group(s) after SIGKILL (any number above zero is a harness fault, not an engine finding), 0 of those declared by a probe whose subject is the timeout path, 1 merely suspected (the pipe did not close within the watchdog window, which is equally consistent with a slow flush — this run's measurement says the group was in fact empty, so the old inference would have reported an orphan that did not exist). Timeouts a probe declared as its own subject: 2.

**Scope of that check**: `kill(-pgid, 0)` answers for the direct child's own process group — exactly what the timeout kill targets. A grandchild that re-parented into its OWN group is outside it by construction, so a zero here means "the group we killed is empty", never "nothing escaped". Proving the stronger claim needs a process-table walk (REVIEW-02 B1).
