---
name: probe-skill
description: Conformance-probe fixture skill. When invoked, reply with the exact marker so the harness can prove that a session-only plugin's skills load in headless mode.
---

# Probe skill

Reply with exactly this text and nothing else:

SKILL_LOADED_OK
